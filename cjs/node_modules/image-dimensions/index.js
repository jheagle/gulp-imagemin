"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.imageDimensionsFromData = imageDimensionsFromData;
exports.imageDimensionsFromStream = imageDimensionsFromStream;
var _png = _interopRequireDefault(require("./types/png.js"));
var _jpeg = _interopRequireDefault(require("./types/jpeg.js"));
var _gif = _interopRequireDefault(require("./types/gif.js"));
var _webp = _interopRequireDefault(require("./types/webp.js"));
var _avif = _interopRequireDefault(require("./types/avif.js"));
var _heic = _interopRequireDefault(require("./types/heic.js"));
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function imageDimensionsFromData(bytes) {
  // The shortest signature is 3 bytes.
  if (bytes.length < 3) {
    return;
  }

  // Prevent issues with Buffer being passed. Seems to be an issue on Node.js 20 and later.
  bytes = new Uint8Array(bytes);

  // Note: Place types that can be detected fast first.
  return (0, _png.default)(bytes) ?? (0, _gif.default)(bytes) ?? (0, _jpeg.default)(bytes) ?? (0, _webp.default)(bytes) ?? (0, _avif.default)(bytes) ?? (0, _heic.default)(bytes);
}

/**
 * Web streams are not async iterable in Safari (iOS, Desktop) as of 2026-05-10
 *
 * @see {@link https://caniuse.com/wf-async-iterable-streams}
 */
async function* asyncIterableFromStream(stream) {
  const reader = stream.getReader();
  let finished = false;
  try {
    while (!finished) {
      // eslint-disable-next-line no-await-in-loop -- Chunks must be read in order to know `done`
      const {
        done,
        value
      } = await reader.read();
      if (done) {
        finished = true;
        break;
      }
      yield value;
    }
  } finally {
    // Cancel stream if consumer exited early
    if (!finished) {
      await reader.cancel();
    }
    reader.releaseLock();
  }
}
async function imageDimensionsFromStream(stream) {
  let buffer = new Uint8Array(0);
  const asyncIterableStream = Symbol.asyncIterator in stream ? stream : asyncIterableFromStream(stream);
  for await (const chunk of asyncIterableStream) {
    // Merge chunks
    const newBuffer = new Uint8Array(buffer.length + chunk.length);
    newBuffer.set(buffer);
    newBuffer.set(chunk, buffer.length);
    buffer = newBuffer;
    const dimensions = imageDimensionsFromData(buffer);
    if (dimensions) {
      return dimensions;
    }
  }
}