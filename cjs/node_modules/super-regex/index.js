"use strict";

require("core-js/modules/esnext.weak-map.delete-all.js");
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.firstMatch = firstMatch;
exports.firstMatchAsync = firstMatchAsync;
exports.isMatch = isMatch;
exports.isMatchAsync = isMatchAsync;
exports.matches = matches;
exports.matchesAsync = matchesAsync;
var _index = _interopRequireWildcard(require("../function-timeout/index.js"));
var _index2 = _interopRequireDefault(require("../time-span/index.js"));
var _index3 = _interopRequireWildcard(require("../make-asynchronous/index.js"));
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function _interopRequireWildcard(e, t) { if ("function" == typeof WeakMap) var r = new WeakMap(), n = new WeakMap(); return (_interopRequireWildcard = function (e, t) { if (!t && e && e.__esModule) return e; var o, i, f = { __proto__: null, default: e }; if (null === e || "object" != typeof e && "function" != typeof e) return f; if (o = t ? n : r) { if (o.has(e)) return o.get(e); o.set(e, f); } for (const t in e) "default" !== t && {}.hasOwnProperty.call(e, t) && ((i = (o = Object.defineProperty) && Object.getOwnPropertyDescriptor(e, t)) && (i.get || i.set) ? o(f, t, i) : f[t] = e[t]); return f; })(e, t); }
const resultToMatch = result => ({
  match: result[0],
  index: result.index,
  groups: result.slice(1),
  namedGroups: result.groups ?? {},
  input: result.input
});
const context = {};
const normalizeTimeout = timeout => {
  if (timeout === undefined || Number.isNaN(timeout)) {
    return undefined;
  }

  // Convert to positive integer, minimum 1
  const normalized = Math.max(1, Math.trunc(Math.abs(timeout)));

  // Handle Infinity
  if (!Number.isFinite(normalized)) {
    return undefined;
  }
  return normalized;
};
function isMatch(regex, string, {
  timeout,
  throwOnTimeout
} = {}) {
  try {
    return (0, _index.default)(() => structuredClone(regex).test(string), {
      timeout: normalizeTimeout(timeout),
      context
    })();
  } catch (error) {
    if ((0, _index.isTimeoutError)(error) && !throwOnTimeout) {
      return false;
    }
    throw error;
  }
}
function firstMatch(regex, string, {
  timeout,
  throwOnTimeout
} = {}) {
  try {
    const result = (0, _index.default)(() => structuredClone(regex).exec(string), {
      timeout: normalizeTimeout(timeout),
      context
    })();
    if (result === null) {
      return;
    }
    return resultToMatch(result);
  } catch (error) {
    if ((0, _index.isTimeoutError)(error) && !throwOnTimeout) {
      return;
    }
    throw error;
  }
}
function matches(regex, string, {
  timeout = Number.POSITIVE_INFINITY,
  matchTimeout = Number.POSITIVE_INFINITY,
  throwOnTimeout
} = {}) {
  if (!regex.global) {
    throw new Error('The regex must have the global flag, otherwise, use `firstMatch()` instead');
  }
  return {
    *[Symbol.iterator]() {
      try {
        const matches = string.matchAll(structuredClone(regex)); // The regex is only executed when iterated over.
        let remainingTimeout = timeout;
        while (true) {
          // Stop iteration if total timeout has been exceeded
          // This prevents normalizeTimeout from converting negative remainingTimeout back to positive
          if (timeout !== Number.POSITIVE_INFINITY && remainingTimeout <= 0) {
            if (throwOnTimeout) {
              const timeoutError = new Error('Script execution timed out');
              timeoutError.code = 'ERR_SCRIPT_EXECUTION_TIMEOUT';
              throw timeoutError;
            }
            return;
          }

          // `matches.next` must be called within an arrow function so that it doesn't lose its context.
          const normalizedTimeout = timeout !== Number.POSITIVE_INFINITY || matchTimeout !== Number.POSITIVE_INFINITY ? normalizeTimeout(Math.min(remainingTimeout, matchTimeout)) : undefined;
          const nextMatch = (0, _index.default)(() => matches.next(), {
            context,
            timeout: normalizedTimeout
          });
          const end = (0, _index2.default)();
          const {
            value,
            done
          } = nextMatch();
          remainingTimeout -= Math.ceil(end());
          if (done) {
            break;
          }
          yield resultToMatch(value);
        }
      } catch (error) {
        if ((0, _index.isTimeoutError)(error) && !throwOnTimeout) {
          return;
        }
        throw error;
      }
    }
  };
}

// Async worker functions
// These run in worker threads (via make-asynchronous) to enable true timeout support in browsers.
// Workers run in isolation and cannot access outer scope, so the Match object structure
// must be duplicated here instead of using resultToMatch(). If the Match structure changes,
// update it in: resultToMatch (line 5), firstMatchWorker (line 125), and matchesWorker (line 140).
const isMatchWorker = (0, _index3.default)((source, flags, string) => {
  const regex = new RegExp(source, flags);
  return regex.test(string);
});
const firstMatchWorker = (0, _index3.default)((source, flags, string) => {
  const regex = new RegExp(source, flags);
  const result = regex.exec(string);
  if (result === null) {
    return;
  }

  // Match object structure - keep in sync with resultToMatch and matchesWorker
  return {
    match: result[0],
    index: result.index,
    groups: result.slice(1),
    namedGroups: result.groups ?? {},
    input: result.input
  };
});
const matchesWorker = (0, _index3.makeAsynchronousIterable)(function* (source, flags, string) {
  const regex = new RegExp(source, flags);
  const matches = string.matchAll(regex);
  for (const match of matches) {
    // Match object structure - keep in sync with resultToMatch and firstMatchWorker
    yield {
      match: match[0],
      index: match.index,
      groups: match.slice(1),
      namedGroups: match.groups ?? {},
      input: match.input
    };
  }
});
const handleAsyncTimeout = (controller, timeout) => {
  if (timeout === undefined || Number.isNaN(timeout)) {
    return;
  }

  // Handle edge cases for setTimeout
  // 0, negative, or Infinity should not set a timeout
  if (timeout <= 0 || !Number.isFinite(timeout)) {
    if (timeout <= 0) {
      // Immediately abort for 0 or negative
      controller.abort();
    }

    // For Infinity or non-finite, don't set timeout (never times out)
    return () => {}; // No-op cleanup
  }
  const timeoutId = setTimeout(() => {
    controller.abort();
  }, timeout);
  return () => {
    clearTimeout(timeoutId);
  };
};
const handleAsyncError = (error, throwOnTimeout, defaultValue) => {
  if (error.name === 'AbortError') {
    if (throwOnTimeout) {
      const timeoutError = new Error('Timed out');
      timeoutError.name = 'TimeoutError';
      throw timeoutError;
    }
    return defaultValue;
  }
  throw error;
};

// Helper to reduce duplication in async methods
const withAsyncTimeout = async (workerFunction, timeout, throwOnTimeout, defaultValue) => {
  const controller = new AbortController();
  const cleanup = handleAsyncTimeout(controller, timeout);
  try {
    const result = await workerFunction(controller.signal);
    cleanup?.();
    return result;
  } catch (error) {
    cleanup?.();
    return handleAsyncError(error, throwOnTimeout, defaultValue);
  }
};
async function isMatchAsync(regex, string, {
  timeout,
  throwOnTimeout
} = {}) {
  return withAsyncTimeout(signal => isMatchWorker.withSignal(signal)(regex.source, regex.flags, string), timeout, throwOnTimeout, false);
}
async function firstMatchAsync(regex, string, {
  timeout,
  throwOnTimeout
} = {}) {
  return withAsyncTimeout(signal => firstMatchWorker.withSignal(signal)(regex.source, regex.flags, string), timeout, throwOnTimeout, undefined);
}
async function* matchesAsync(regex, string, {
  timeout,
  throwOnTimeout
} = {}) {
  if (!regex.global) {
    throw new Error('The regex must have the global flag, otherwise, use `firstMatchAsync()` instead');
  }
  const controller = new AbortController();
  const cleanup = handleAsyncTimeout(controller, timeout);
  try {
    for await (const match of matchesWorker.withSignal(controller.signal)(regex.source, regex.flags, string)) {
      yield match;
    }
  } catch (error) {
    return handleAsyncError(error, throwOnTimeout, undefined);
  } finally {
    cleanup?.();
  }
}